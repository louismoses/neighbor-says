<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Neighbor Says!</title>
    <link rel="stylesheet" href="styles/main.css">
</head>
<body>
    <h1> <%= affirm %></h1>
    <% if(locals.currentWeather){ %>
        <h2> <%= currentWeather.current.condition.text %></h2>
        <% } %>
    <form action="/submit" method="POST">
        <label for="weatherQuestion">Dude whats the weather today</label>
        <input type="text" name="cityWeather" placeholder="City Name">
        <input type="submit" value="submit">
    </form>
</body>
</html>